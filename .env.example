# Copie este archivo a ".env" (en la raíz del proyecto) y ajuste los
# valores a su entorno local. El archivo ".env" real NUNCA debe subirse
# al repositorio (ver .gitignore).

DB_HOST=localhost
DB_PUERTO=3306
DB_USUARIO=root
DB_PASSWORD=
DB_NOMBRE=sistema-zoofipetss
